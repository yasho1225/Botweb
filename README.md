# BotWeb — website

Student-run initiative: free websites and AI chatbots for nonprofits and school clubs.

Built with [Next.js](https://nextjs.org).

## Getting started

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

## Environment

Copy `.env.example` to `.env.local` and set `NEXT_PUBLIC_FORMSPREE_URL` for the contact form ([Formspree](https://formspree.io)).

## Deploy

Deploy on [Vercel](https://vercel.com/new) or any Node host; add the same env vars in the dashboard.
